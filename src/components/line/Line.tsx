import styled from 'styled-components'
import { COLORS_MAP } from '~/styles/colors.map'

export const Line = styled.div`
  width: 100%;
  height: 0.5px;
  background-color: ${COLORS_MAP.standard.fourthWhite};
`
