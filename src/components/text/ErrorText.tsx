import styled from 'styled-components'
import { COLORS_MAP } from '~/styles/colors.map'

export const ErrorText = styled.div`
  color: ${COLORS_MAP.errors.font};
  font-size: 10px;
  text-align: left;
  margin: 0;
`
